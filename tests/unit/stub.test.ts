import { expect } from 'chai';

describe('calculate B', function () {
  it('add', function () {
    const result = 7;
    expect(result).equal(7);
  });
});

describe('calculate A', function () {
  it('add', function () {
    const result = 7;
    expect(result).equal(7);
  });
});
